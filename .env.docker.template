# Copy to `.env.docker` and run:
#   docker compose --env-file .env.docker up --build
#
# Tips:
# - `docker compose --env-file` is used for compose variable substitution (ports, Postgres init vars, etc.)
# - `docker-compose.yml` also loads this file into the `app` container via `env_file: .env.docker`

# --- Postgres (compose service) ---
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=meteordb
POSTGRES_PORT=5432

# --- Redis (compose service) ---
REDIS_PORT_EXPOSE=6379

# --- App ports ---
APP_PORT=3000

# --- Prisma / Database (app runtime) ---
# Use the compose service name `postgres` as host.
DATABASE_URL="postgresql://postgres:postgres@postgres:5432/meteordb?schema=public"
# Optional: direct connection (often same as DATABASE_URL in docker compose)
DIRECT_URL="postgresql://postgres:postgres@postgres:5432/meteordb?schema=public"

# --- Redis Cache (app runtime) ---
# Use the compose service name `redis` as host.
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_USERNAME=default
REDIS_PASSWORD=
REDIS_DB=0
# Optional: full URL; if set it will take precedence in code
# REDIS_URL="redis://default:password@redis:6379/0"
# Optional: disable cache entirely
# REDIS_DISABLED=true

# --- NextAuth.js ---
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=change-me

# --- Supabase (public) ---
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# --- Email ---
RESEND_API_KEY=

# --- AWS S3 / CloudFront ---
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=ap-northeast-2
AWS_S3_BUCKET_NAME=
AWS_CLOUDFRONT_DOMAIN=

# --- AI (optional) ---
AI_PROVIDER=openai
AI_MODEL=
AI_ROUTER_MODEL=
OPENAI_API_KEY=
GOOGLE_API_KEY=

# Optional: bootstrap schema (this repo currently has no `prisma/migrations/`)
PRISMA_DB_PUSH=true
